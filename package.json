{
  "name": "vox",
  "version": "0.0.0",
  "description": "voxel world",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf build",
    "lint-js": "standard",
    "lint-css": "stylelint **/*.css --config node_modules/stylelint-config-standard/index.js",
    "lint": "npm run lint-js && npm run lint-css",
    "test": "npm run lint && node ./test",
    "build": "npm run clean && mkdirp build && browserify --debug -t brfs src/ > build/bundle.js && cp -r static/* build/",
    "start": "npm run build && node ./src/server"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/dcposch/gl.git"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "bugs": {
    "url": "https://github.com/dcposch/gl/issues"
  },
  "dependencies": {
    "express": "^4.14.0",
    "game-shell": "^1.3.2",
    "gl-mat4": "^1.1.4",
    "gl-vec4": "^1.0.1",
    "murmurhash": "0.0.2",
    "regl": "^1.2.1",
    "ws": "^1.1.1"
  },
  "devDependencies": {
    "brfs": "^1.4.3",
    "browserify": "^13.1.0",
    "fs-extra": "^1.0.0",
    "http-server": "^0.9.0",
    "mkdirp": "^0.5.1",
    "pngjs": "^3.0.0",
    "rimraf": "^2.5.4",
    "standard": "^8.3.0",
    "stylelint": "^7.3.1",
    "stylelint-config-standard": "^13.0.2",
    "tape": "^4.6.2"
  }
}
